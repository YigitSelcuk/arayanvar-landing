server {
    listen 80;
    server_name 104.247.173.172;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;

    location / {
        proxy_pass http://localhost:3002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Cache static assets
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        proxy_pass http://localhost:3002;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}

# SSL Configuration (uncomment after getting SSL certificate)
# server {
#     listen 443 ssl http2;
#     server_name arayanvar.com www.arayanvar.com;
#
#     ssl_certificate /etc/letsencrypt/live/arayanvar.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/arayanvar.com/privkey.pem;
#
#     # SSL settings
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     # Same location blocks as above
#     location / {
#         proxy_pass http://localhost:3001;
#         # ... rest of proxy settings
#     }
# }
#
# # Redirect HTTP to HTTPS
# server {
#     listen 80;
#     server_name arayanvar.com www.arayanvar.com;
#     return 301 https://$server_name$request_uri;
# }
